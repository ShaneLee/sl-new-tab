Any CSS styles should go in the css/style.css file and not inline. You should use the variables for
the colours so the themes work. You should use the environment.js to interact with the API unless
this is a background script
